# /translate

Skill placeholder. Debe implementarse respetando `CLAUDE.md`, aplicando la metodología inmutable de `context/` y leyendo el contexto específico disponible en `brand_context/`. Nunca escribir datos de una marca dentro de `context/`.
